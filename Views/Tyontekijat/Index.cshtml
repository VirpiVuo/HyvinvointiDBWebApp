@model IEnumerable<Hyvinvointisovellus.Tyontekijat>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Etuimi)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Sukunimi)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Etuimi)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Sukunimi)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.TyontekijaID }) |
            @Html.ActionLink("Details", "Details", new { id=item.TyontekijaID }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.TyontekijaID })
        </td>
    </tr>
}

</table>
